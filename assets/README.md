# assets

Drop profile images here.

## banner.png  <<< REPLACE ME >>>
Optional header banner shown at the top of the profile README.

- Recommended size: ~1280 x 320 px (any wide ratio works), PNG or JPG.
- Save it as `banner.png` in this folder.
- Then open `../README.md` and UNCOMMENT the `<img src="assets/banner.png" ...>` line near the top.

## Round avatar (not stored here)
Your circular profile photo is NOT set from this repo. Upload it in:
GitHub → Settings → Profile → Profile picture.
